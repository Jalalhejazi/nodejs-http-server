{
  "name": "nodejs-http-server",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "prestart": "npm install --silent",
    "start": "nodemon server.js"
  }
}
